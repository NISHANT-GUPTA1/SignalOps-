# Shell scripts must use LF line endings or they break on Linux (the \r in a
# CRLF shebang makes the interpreter path "not found"). Force LF regardless of
# the checkout platform.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
